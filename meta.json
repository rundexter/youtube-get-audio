/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [{
        "id"          : "url",
        "title"       : "Url",
        "description" : "The YouTube URL to parse"
    }, {
        "id"          : "file_folder",
        "title"       : "File Folder",
        "description" : "Path to the folder where your mp3(s) should be stored."
    }],
    //Required. Outputs the module emits.
    "outputs": [{
        "id"          : "path",
        "title"       : "Path",
        "description" : "Path"
    }],
    "providers": ["dropbox"],
    "title": "YouTube Audio To Dropbox" /* Required.A display name for the module */
}
